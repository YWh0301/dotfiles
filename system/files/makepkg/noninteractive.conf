# Keep the normal Arch makepkg configuration, but never let a background build
# block waiting for a password. system/apply maintains the sudo timestamp.
source /etc/makepkg.conf
PACMAN_AUTH=(sudo -n)
